## Flux UI Component Library

### Styling Guidelines
- Flux UI provides pre-styled components with built-in colors, backgrounds, and styles
- **Do not set custom colors or text colors** unless explicitly required to stand out
- **Text elements (headings, text, labels) have no color variants** - use them as-is
- Interactive components (buttons, badges, etc.) use semantic variants when documented (e.g., `variant="primary"`, `variant="danger"`)
- Trust the component defaults - they're designed to work together
- Avoid custom CSS classes on Flux components unless absolutely necessary

### Layout Structure
- **Never wrap page content in `<div>` containers**
- Use `<flux:main>` for full-width layouts
- Use `<flux:main container>` for centered, max-width layouts
- Follow Flux's layout patterns from `/docs/flux-pro/layouts/`

### Component Usage
- Check `/docs/flux-pro/components/[component-name]` for complete component documentation
- **Each component has a reference table listing all available attributes** - use it
- Only use attributes that are listed in the reference table
- Follow the documented examples exactly - syntax matters
- If a feature isn't in the reference table, it doesn't exist - ask before assuming
- Use `variant="listbox"` for `<flux:select>` unless another variant is explicitly required or specified

### Forms
- For complete forms, do not build fields with shorthand label props or inline-only form elements
- Wrap each real form control in `<flux:field>` and include an explicit `<flux:label>`, the input/select/textarea/checkbox control, and `<flux:error name="...">`
- Use `variant="inline"` only for small single-row controls such as one checkbox or switch with its label; do not use inline fields as the general structure for full forms
- Place form action buttons below the form fields, typically in a trailing `<div class="flex justify-end gap-2">`

### Tables
- **NEVER use `align` on `<flux:table.column>`** - only use `align` on `<flux:table.cell>`

### Modals
- Use `<flux:modal.trigger name="...">` with a matching `<flux:modal name="...">` for opening modals
- Modal names MUST be unique when modals are rendered inside loops
- Prefer Flux modal events like `@close="someLivewireAction"` and `@cancel="someLivewireAction"` for cleanup, reset, or cancellation logic
- Use `<flux:modal.close>` for cancel/close buttons inside modal content
- Use `:dismissible="false"` when clicking outside must not close the modal
- Do not use browser confirmations such as `wire:confirm` for destructive actions when a Flux confirmation modal is appropriate

### Livewire Integration
- Flux components work seamlessly with Livewire
- Use `wire:model`, `wire:click` etc. as shown in component examples
